language: python
python:
  - "3.6"
  # PyPy versions
  - "pypy-5.3.1"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:  python -m unittest discover -s pymsascoring -p '*_test.py'